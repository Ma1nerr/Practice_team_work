import React from "react";

const Header = () => {
  return (
    <header style={styles.header}>
      <h1>Our team 5</h1>
    </header>
  );
};

const styles = {
  header: {
    padding: "3em",
    color: "#00ff91ff",
    textAlign: "center",
    fontWeight: "bold",
  },
};

export default Header;
